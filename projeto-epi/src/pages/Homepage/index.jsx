import './styles.css'

export default function HomePage(params) {
    return (
        <div className="homepage">
            <h1>Bem vindo a pagina inicial</h1>
            <p>Use a navegação acima para cadastrar a listar usuario.</p>
        </div>
    );
}